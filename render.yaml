services:
    - type: web
    name: 20y-core-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api_20y:app --host 0.0.0.0 --port 10000"
    envVars:
            - key: PYTHON_VERSION
        value: 3.11.0
